## 为什么用pip比用easy_install的好?

`pip` 是对 `easy_install` 以下方面进行了改进：

*   所有的包是在安装之前就下载了，所以不会出现只安装了一部分的情况
*   在终端上的输出更加友好
*   对于动作的原因会进行持续的跟踪
*   错误信息会非常有用
*   代码简洁精悍可以很好地编程
*   不必作为 egg 存档，能扁平化安装（仍然保存 egg 元数据）
*   原生的支持其他版本控制系统（Git、Mercurial、Bazaar）
*   卸载包
*   可以简单地定义修改一系列的安装依赖等